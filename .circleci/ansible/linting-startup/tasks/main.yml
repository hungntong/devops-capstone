---
- name: "Copy dist to EC2 server"
  become: yes
  copy:
    src: ~/project/docker/
    dest: /home/ubuntu/

- name: "hadolint Dockerfile"
  command: hadolint /home/ubuntu/Dockerfile
  register: output

